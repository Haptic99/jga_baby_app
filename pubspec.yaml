name: jga_baby_app
description: "Ein JGA Tamagotchi für den Bräutigam."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Das Herzstück für die Datenverwaltung
  provider: ^6.1.1
  # Die Sensoren für das Grinder-Minispiel
  sensors_plus: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true